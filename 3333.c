#include<stdio.h>
#include<math.h>

int main (void){
    //pow함수
    int a =0;
    a =pow(3, 5);

    //반복문 표현
    int base;
    int result = 1;
    for (int i = 0; i < exp; i++)
    {
        result = result * base;
    }
    
    printf("%d\n",a);
    printf("%d",result);
    return 0;
}