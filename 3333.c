#include<stdio.h>

int main(void){
    int A[] = {11};
    int B[] = {11};
    int count = 0;
    int plus = 0;
    while(B[count] = 0){
        scanf("%d",A+count);
        scanf("%d",B+count);
        ++count;
        }
    
    
    for (int i = 0; i < count; i++)
    {
        plus = A[i] + B[i];
        printf("%d\n",plus);
    }
    
    return 0;
}